let banco = ['Sofia', 'David', 'Juan']
banco.splice(2, 0, 'Sara', 'Agustin')
banco.splice(0, 1)
banco.splice(1, 0, 'Renata')
banco.splice(4, 0, 'Elena')
console.log(banco)

function piramide (numPisos){
    for(let x = 0; x < numPisos; x++){
    let primero = ''
    for (let i = 0; i < x + 1; i++){
        primero = primero + '*'
    }
    console.log(piso)
}
}
piramide(10)
