function miSuma (a, b){
    console.log(`tu resultado es ${a + b}`)
    return a + b 
}
miSuma(8, 9)

function miSuma1 (a, b){
    return (a + b)
}
console.log(`El producto es ${miprducto(4, 5)}`)
let res
function miResta (a, b){
    return (a - b)
}
res = miResta(10, 4)
console.log(`La resta es ${res}`)

function miName (nombre, paterno, materno){
    console.log(`Mi nombre es ${nombre} ${paterno} ${materno}`)
}
miName(Erick, Hernandez, Monteagudo)

function division (a, b){
    console.log(`El resultado de dividir ${a} entre ${b} es ${a / b}`)
}

function cociente (a, b){
    console.log(`El cociente de ${a} entre ${b} es ${a % b}`)
}